<h2>welcome workouts#index</h2>
<% @workouts.each do |workout| %>
  <p><h3><%=link_to workout.date.strftime(" %d %B %Y"), workout_path(workout)%></h3></p>
  <p><%= workout.workout%></p>
  <p><%= workout.mood%></p>
  <p><%= workout.length%></p>
  <br/>
<% end %>